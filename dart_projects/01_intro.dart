//Basic dart syntax showcase
void main() {
  for (int i = 0; i < 10; i++) {
    //Dart has optionally typing so you can use "var" or "int" or a concrete type.
    print('hello ${i + 1}'); //Printing works simillar to JS
  }
}
